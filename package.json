{
  "name": "@alexandremulina90/file-parser",
  "version": "1.0.0",
  "description": "File parser",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "format": "prettier --write \"src/**/*.ts\" \"src/**/*.js\"",
    "lint": "tslint -p tsconfig.json",
    "prepare": "npm run build",
    "version": "npm run format && git add -A src",
    "postversion": "git push && git push --tags"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/alexandremulina/file-parser.git"
  },
  "keywords": [
    "csv"
  ],
  "author": "Alexandre Mulina",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/alexandremulina/file-parser/issues"
  },
  "homepage": "https://github.com/alexandremulina/file-parser#readme",
  "devDependencies": {
    "@types/node": "^17.0.25",
    "prettier": "^2.6.2",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^4.6.3"
  },
  "files": [
    "lib/**/*"
  ]
}
